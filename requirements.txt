# Google Maps Scraper — Python Dependencies
# Author    : Soubarna Karmakar
# Copyright : © 2025 Soubarna Karmakar. All rights reserved.
#
# Install: pip install -r requirements.txt

# ── Web framework + ASGI server ───────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
python-multipart>=0.0.9

# ── Desktop window (wraps the React UI — no browser bar) ─────────────────────
# pywebview is OPTIONAL — the app falls back to opening your default browser.
# It requires .NET SDK on Windows and only has pre-built wheels for Python 3.10–3.12.
# Install separately ONLY if you are on Python 3.10, 3.11 or 3.12:
#   pip install pywebview
# DO NOT add it here — it breaks Python 3.13+ installs via pip.

# ── Browser automation ────────────────────────────────────────────────────────
playwright>=1.43.0

# ── Excel export ──────────────────────────────────────────────────────────────
openpyxl>=3.1.2

# ── Email extraction from websites (optional) ─────────────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.3
lxml>=5.2.0
